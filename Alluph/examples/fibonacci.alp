function int _fibonacci(int target) do

    int a;
    int b;
    int next;

    a = 1;
    b = 1;
    
    while next <= target do

        next = a + b;
        a = b;
        
        print(a ++ ", ");
        
        b = next;

    end

    print(a);
    
    return 1;
end

function int _main() do

    int number;
    int result;

    read(number);

    result = _fibonacci(number);

    return 0;

end